@model IEnumerable<Webbshop.Models.Target_Group>
@using Webbshop.Models;
@{
    var user = Session["ViewUser"] as ViewUser;
}

@if (user != null)
{
    if (user.Email != "admin")
    {
        { Response.Redirect("~/Shop/Index"); }
    }
}
else
{
    { Response.Redirect("~/Shop/Index"); }
}
@{
    ViewBag.Title = "Index";
}

<h2>Målgrupp</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Target_Group1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Target_Group1)
        </td>
        <td>
            @Html.ActionLink("Redigera", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Detaljer", "Details", new { id=item.Id }) |
            @Html.ActionLink("Ta bort", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

<input type="button" class="btn btn-default" value="Lägg till ny målgrupp" onclick="location.href='@Url.Action("Create", "Target_Group")'" style="margin: 0px 5px 5px 5px;" />
